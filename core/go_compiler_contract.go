package core

// GoCgoImportPath is the Go compiler's foreign namespace. It is not a module
// dependency or a product-owned package, and has no source directory of its own.
const GoCgoImportPath = "C"
