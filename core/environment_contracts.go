package core

const (
	// EnvironmentHomeName is the one exact variable that binds a child home.
	EnvironmentHomeName = "HOME"
	// EnvironmentTemporaryName is the one exact variable that binds temporary files.
	EnvironmentTemporaryName = "TMPDIR"
	// EnvironmentCacheName is the one exact variable that binds the generic cache root.
	EnvironmentCacheName = "XDG_CACHE_HOME"
)
