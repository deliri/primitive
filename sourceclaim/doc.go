// Package sourceclaim defines streamed human explanations for source subjects.
//
// A claim records why a project, package, or file exists, what problem it
// solves, what benefit it creates, what it owns, and what would permit its
// removal. It does not inspect source and cannot prove itself. An offline
// compiler applies the agreement and sourceproof binds claims to independent
// observations without Primitive learning the inspected product.
package sourceclaim
